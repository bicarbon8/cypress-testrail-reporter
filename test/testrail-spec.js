const chai = require("chai");
const expect = chai.expect;
const TestRail = require('../src/testrail');

describe('TestRail', () => {
    it('can create a new Test Plan', async () => {
        // TBD
    }).skip();
});